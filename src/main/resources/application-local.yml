spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/board
    username: root
    password: ${DB_PASSWORD}
  sql:
    init:
      mode: never

jwt:
  secret-key:

redis:
  host: localhost
  port: 6379
